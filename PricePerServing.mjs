export default class PricePerServing {
  static calculatePricePerServing(price, kCalPerKG) {
    const caloriesPerGram = kCalPerKG / 1000;
    // const priceByWeight =
    // const pricePerCalorie =
  }
}
